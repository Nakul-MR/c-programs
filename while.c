#include <stdio.h>
int main()
{
    int a,b,diff;
    printf("Enter two numbers: ");
    scanf("%d %d", &a,&b);
    while (1)
    {
        
            diff=a-b;
        
        
    
     printf("difference = %d",diff);
     break;
    }
    return 0;
}